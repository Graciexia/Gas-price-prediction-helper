<%= form_for(@gas_grade) do |f| %>
  <% if @gas_grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gas_grade.errors.count, "error") %> prohibited this gas_grade from being saved:</h2>

      <ul>
      <% @gas_grade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
