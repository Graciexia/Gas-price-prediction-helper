<%= form_for(@oil_price) do |f| %>
  <% if @oil_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oil_price.errors.count, "error") %> prohibited this oil_price from being saved:</h2>

      <ul>
      <% @oil_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :oil_price %><br>
    <%= f.text_field :oil_price %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
